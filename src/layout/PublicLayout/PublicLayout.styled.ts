import styled from "styled-components";

export const SPublicLayout = styled.div`
    nav{
        display: inline-flex;
        padding: 12px;
        margin: 12px;
        gap: 12px;
        border: solid 1px;
    }

`